import turtle
from turtle import *
t = Turtle()

t.shape('turtle')
def draw_square_spiral():
    length=5
    for i in range(60):
        for i in range(4):
            t.forward(length)
            t.left(90)
            t.left(5)
        length += 5

turtle.done()