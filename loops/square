import turtle
from turtle import *
t = Turtle()

t.shape('turtle')
for i in range(60):
    def square(x):
        for i in range(4):
            t.forward(x)
            t.left(90)
    square(100)
    t.right(5)

turtle.done()